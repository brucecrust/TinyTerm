#ifndef NUMERICS_H
#define NUMERICS_H

namespace Numerics {

    namespace Windows {

        // Offsets:
        const int standard_offset = 0;
        const int text_window_x_offset = 10;

        // Sizes:
        const int text_window_x = 10;
    }
}

#endif
